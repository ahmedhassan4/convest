/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['isomorphic-furyroad.s3.amazonaws.com', 'flamingo.elli.education', 'dev.elli.education','randomuser.me', 'images.unsplash.com'],
      },
};

export default nextConfig;
